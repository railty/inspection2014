<%= form_for(@attr_template) do |f| %>
  <% if @attr_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attr_template.errors.count, "error") %> prohibited this attr_template from being saved:</h2>

      <ul>
      <% @attr_template.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
